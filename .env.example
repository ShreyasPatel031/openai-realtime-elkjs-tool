OPENAI_API_KEY="<your-key-here>"

# OpenAI API Key for icon fallback service (client-side)
VITE_OPENAI_API_KEY=your_openai_api_key_here

# Note: The icon fallback service uses OpenAI embeddings to find similar icons
# when exact matches fail. This improves user experience by automatically
# suggesting the closest matching icon from the available set.