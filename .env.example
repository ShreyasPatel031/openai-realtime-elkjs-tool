# 🌍 Environment Variables for Architecture Generator
# Copy this file to .env for local development
# Set these same variables in your Vercel dashboard for production

# OpenAI API Key - Required for AI architecture generation
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# Vite-compatible version for browser access (icon fallback service)
# Should be the same as OPENAI_API_KEY
VITE_OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# Note: Both variables should have the same value
# OPENAI_API_KEY is used by server-side API endpoints
# VITE_OPENAI_API_KEY is used by client-side services